export const CartCard = ({image, title}) => {
    return (
        <>
            <div>
                {/* <img src={image} alt={title} /> */}
                <h1>Hello</h1>
            </div>
        </>
    )
}