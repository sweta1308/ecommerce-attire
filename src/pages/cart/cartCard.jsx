export const CartCard = ({title}) => {
    return (
        <>
            <div>
                {/* <img src={image} alt={title} /> */}
                <h1>Hello</h1>
                <p>{title}</p>
            </div>
        </>
    )
}